# LexCore v0.14.1 更新報告

## 本次更新

### 1. 精準背誦訓練改為四階段

1. 閱讀：先完整顯示法條全文，完成閱讀後才進入下一階段。
2. 口訣：顯示法條已設定的口訣／諧音；沒有自訂內容時，會依條文中的數字與關鍵字產生提示。
3. 數字與關鍵字辨識：自動擷取數字、時間、比例，以及「應／得／不得」等容易混淆的字眼，使用選擇題確認記憶。
4. 排序：將條文句子打亂，使用上下移動重新排列，再以完整順序送出判定。

若條文沒有可辨識的數字或關鍵字，第 3 階段會自動略過，直接進入排序。

### 2. 移除考點中心

- 移除側邊欄「考點中心」。
- 移除 `/knowledge` 路由與入口。
- 既有考點資料模型仍保留作為內部資料相容性，避免破壞既有 IndexedDB 資料。

### 3. 法條螢光筆與口訣

法條瀏覽每一條法條新增：

- 「螢光標記」：輸入原文片段後，以黃色螢光顯示重點。
- 「設定口訣」：儲存該條法條專屬的口訣／諧音。
- 已有標記會顯示為可點擊的標記，點擊即可移除。

標記與口訣透過既有 `updateArticle` 儲存在 IndexedDB，重新整理或重新開啟後仍會保留。

## 驗證

- TypeScript：通過
- Vitest：17 個測試檔、48 個測試通過
- Vite production build：通過

## 已知限制

- 螢光筆目前以「輸入原文片段」方式建立標記，尚未加入滑鼠／觸控拖曳選取。
- 自動口訣是提示用途，正式背誦仍以官方法條全文為準。
- 第 3 階段目前以數字與固定關鍵字規則擷取考點，不會自行解釋法律效果。
